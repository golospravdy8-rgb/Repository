# 🎯 ПЛАН РЕАЛИЗАЦИИ: Basketball Admin UI - 4 ЗАДАЧИ

**Дата начала**: 2026-04-18  
**Статус**: PLANNING  
**Приоритет**: Вслед за друг другом (1 → 2 → 3 → 4)

---

## 🎨 ДИЗАЙН И ЭСТЕТИКА — ОБЯЗАТЕЛЬНЫЕ ТРЕБОВАНИЯ

### Принципы дизайна
- **Тёмная тема**: #0d1520 базовый фон (сохранить текущую)
- **Высота кнопок**: одинаковая в пределах одного ряда (по наивысшей)
- **Padding кнопок**: 6px 4px (минимальный)
- **Шрифт кнопок**: 11px, font-weight: 600
- **Цвета (семантические)**:
  - 🟢 **Зелёный** (#1e5c35) = попадание, успех
  - 🔴 **Красный** (#5e1515) = промах, ошибка, потеря
  - 🔵 **Синий** (#1a3560) = нейтральное действие (подборы, передача, перехват)
  - 🟡 **Янтарный** (#6b4e10) = служебные кнопки (тайм-аут, отмена, контекст)
  - 🟣 **Фиолетовый** (#3d1f6b) = подборы в нападении
- **Разделители между группами**: вертикальная линия `1px solid #1a2e40`, margin `0 4px`
- **Разделители между зонами**: горизонтальная линия `1px solid #1a2e40`, padding `4px 0`
- **Активная кнопка**: светлее на 20%, `border: 1px solid` соответствующего цвета

### Высоты блоков (ориентир для 1366×768)
```
┌─ Шапка (команды, счёт, таймер): 60px
├─ ЗОНА 1 (управление игрой): 36px
├─ ЗОНА 2 (броски, 2 ряда): 80px
├─ ЗОНА 3 (блок/потеря/фолы): 36px
├─ ЗОНА 4 (контекст броска): 30px
├─ Имя выбранного игрока: 28px
├─ ────────────────────────────────
├─ Боковые панели игроков: calc(100vh - 60px - 320px)
├─ Лог действий: 80px (с вертикальным скроллом)
└─ ИТОГО ЦЕНТРАЛЬНАЯ ПАНЕЛЬ: ~270px + боки + лог
```

**Жёсткое требование**: Всё должно помещаться на экране 1366×768 **БЕЗ прокрутки страницы** (только лог внутри себя скроллится).  
**Если блоки не помещаются** — уменьшай padding, не убирай кнопки.

### Стиль кнопок (единый шаблон)
```typescript
// Базовый стиль для ВСЕХ кнопок
style={{
  border: "none",
  borderRadius: 5,
  padding: "6px 4px",
  fontSize: 11,
  fontWeight: "600",
  cursor: disabled ? "not-allowed" : "pointer",
  background: disabled ? "#1a2e40" : "COLOR_FROM_SEMANTIC_TABLE",
  color: disabled ? "#4a7fa5" : "#ffffff",
  opacity: disabled ? 0.5 : 1,
  transition: "all 0.15s ease"
}}

// При активности (для кнопок контекста броска)
style={{
  ...baseStyle,
  border: isActive ? "1px solid #e8a030" : "1px solid #1a2e40",
  background: isActive ? "COLOR_BRIGHT" : "COLOR_DIM"
}}
```

### Разделители (код)
```typescript
// Вертикальный разделитель между группами кнопок
<div style={{
  width: "1px",
  height: "32px",
  background: "#1a2e40",
  margin: "0 4px"
}} />

// Горизонтальный разделитель между зонами
<div style={{
  height: "1px",
  background: "#1a2e40",
  margin: "4px 0"
}} />
```

---

## 📋 ЛОГ ДЕЙСТВИЙ — ТРЕБОВАНИЯ

### Поведение лога
- **Новые записи добавляются сверху** (последнее действие первым в списке)
- **Последняя запись всегда видна** — auto-scroll к последнему события при добавлении
- **Высота**: фиксированная ~80px, `overflowY: auto`
- **При переполнении**: появляется скролл справа

### Структура одной записи
```
Q1 07:47 · #4 Одинець З. · +2 попав
│    │     │        │      │
время четвер│    номер+фамилия  действие (цветное)
```

**Формат времени**: `Q{quarter} {MM:SS}`  
**Формат игрока**: `#{number} {lastName} {firstName[0]}.`  
**Действие**: зависит от типа события

### Цветовая схема лога
- 🟢 **+1/+2/+3 попав**: #4ef472 (зелёный)
- 🔴 **×2/×3 промах, потеря**: #f47a7a (красный)
- 🔵 **Передача, подбор**: #5ae8f4 (синий)
- 🟡 **Фол**: #f4cc5a (янтарный)
- 🟠 **Блок**: #f4cc5a (янтарный)

### CSS лога
```typescript
style={{
  height: "80px",
  background: "#080f18",
  border: "1px solid #1a2e40",
  borderRadius: 6,
  padding: "6px 8px",
  overflowY: "auto",
  display: "flex",
  flexDirection: "column-reverse", // Последнее событие сверху
  gap: "2px",
  fontSize: "10px",
  color: "#5a8fb0",
  fontFamily: "monospace"
}}
```

---

## 👥 СПИСОК ИГРОКОВ — ФИНАЛЬНЫЕ ТРЕБОВАНИЯ

### Видимость и высоты
- **12 игроков (5 паркет + 7 лавка)**: все видны БЕЗ скролла
- **Высота строки**: 24px (строго! для touch-comfort)
- **Шрифт**: 11px
- **Номер**: bold, min-width: 20px, fixed width

### Выделение выбранного игрока
- **Фон**: #163a5c (тёмно-синий)
- **Цвет текста**: #5ab3f4 (светлый синий)
- **Бордер**: нет (чистое выделение фоном)

### Секции (заголовки)
- **"На паркеті"**: зелёная точка (●), текст 10px uppercase, цвет #2ecc71
- **"Лавка"**: серая точка (○), текст 10px uppercase, цвет #4a7fa5

### Правило для скролла
- **overflowY: auto** на контейнере (если вдруг что-то вылезет)
- **overflow: hidden ЗАПРЕЩЁН** (это скрывает игроков)

---

## ✅ ФИНАЛЬНЫЙ ЧЕКЛІСТ ДИЗАЙНА

После реализации открой https://basketball.lviv.ua/admin/games/28 и убедись:

- [ ] ✅ Всё помещается на экране 1366×768 без вертикальной прокрутки страницы
- [ ] ✅ 12 игроков видны без скролла в боковых панелях (левая + правая)
- [ ] ✅ Лог внизу с вертикальным скроллом, последнее действие всегда видно сверху
- [ ] ✅ Кнопки компактные (6px 4px padding), одинаковой высоты в пределах ряда
- [ ] ✅ Цвета семантические: зелень = успех, красный = ошибка, синий = нейтраль
- [ ] ✅ Разделители между группами кнопок: вертикальные линии (1px #1a2e40)
- [ ] ✅ Разделители между зонами: горизонтальные линии (1px #1a2e40)
- [ ] ✅ При выборе игрока: кнопки зон 2–3 становятся активными (светлеют, бордер появляется)
- [ ] ✅ Зона 4 (контекст броска): активный выбор отмечен border + светлый фон
- [ ] ✅ Шрифты: 11px для кнопок, 10px для заголовков, 10px для лога

---

## ЗАДАЧА 1: Переработка центральной панели (ЗОНЫ 1-4)

### Описание
Заменить текущую раскладку кнопок на 4-зонную структуру с правильным размещением попаданий/промахов, фолов, тайм-аутов и контекста броска. **Выдержать единый дизайн** согласно разделу "ДИЗАЙН И ЭСТЕТИКА" выше.

### Файл для изменения
- `components/live-tracker/LiveScoreTracker.tsx` — внутри контейнера CENTER (строки ~402-923)

### ЗОНА 1: Управление игрой (36px высота)
```
[ Пауза ]  [ Період ]  |  [ Тайм-аут ]  |  [ Скасувати ]  |  [ Завершити ]
```

**Кнопки:**
- **Пауза** — синий (#1a3560), вызывает `pauseGame(gameId)`
- **Період** — синий (#1a3560), переход к следующему периоду (nextQuarter)
- `|` — вертикальный разделитель (1px #1a2e40, margin 0 4px)
- **Тайм-аут** — янтарный (#6b4e10), новая функция:
  ```typescript
  onClick={() => {
    if (selectedTeamId === game.homeTeamId) {
      setHomeTimeouts(p => Math.max(0, p - 1));
      recordTimeoutEvent(gameId, selectedTeamId);
    } else {
      setAwayTimeouts(p => Math.max(0, p - 1));
      recordTimeoutEvent(gameId, selectedTeamId);
    }
  }}
  ```
- **Скасувати** — янтарный (#6b4e10), вызывает `undoLastEvent(gameId)`
- **Завершити** — красный (#dc2626), вызывает `endGame(gameId)` (как сейчас)

**Дизайн:**
- Все кнопки: 36px высота, padding 6px 4px, font-weight 600
- Разделители: 1px solid #1a2e40, margin 0 4px

### ЗОНА 2: Броски и основная статистика (80px, 2 ряда)

```
ВЕРХНИЙ РЯД (попадания):
[ +2 попав ]  [ +3 попав ]  |  [ Підбір З ]  [ Підбір Н ]  |  [ Штр. влучив ]

ГОРИЗОНТАЛЬНЫЙ РАЗДЕЛИТЕЛЬ (1px #1a2e40, margin 4px 0)

НИЖНИЙ РЯД (промахи и нейтральное):
[ ×2 промах ]  [ ×3 промах ]  |  [ Передача ]  [ Перехват ]  |  [ Штр. промах ]
```

**Кнопки и цвета:**

| Кнопка | Цвет | Код | Функция |
|--------|------|-----|---------|
| +2 попав | Зелёный | #1e5c35 | addScoreWithType(2, eventType) |
| +3 попав | Зелёный | #1e5c35 | addScoreWithType(3, eventType) |
| Підбір З | Фіолет | #3d1f6b | addReboundDef() |
| Підбір Н | Фіолет | #3d1f6b | addReboundOff() |
| Штр. влучив | Зелёный | #1e5c35 | addScoreWithType(1, eventType) |
| ×2 промах | Червоний | #5e1515 | addMissFg2() |
| ×3 промах | Червоний | #5e1515 | addMissFg3() |
| Передача | Синій | #1a3560 | addAssist() |
| Перехват | Синій | #1a3560 | addSteal() |
| Штр. промах | Червоний | #5e1515 | addMissFt() |

**Дизайн:**
- Верхний ряд: 36px высота, 2 ряда
- Нижний ряд: 36px высота
- Каждая кнопка: padding 6px 4px
- Разделители между группами: вертикальные 1px #1a2e40, margin 0 4px

### ЗОНА 3: Блок / Потеря / Фолы (36px высота)

```
[ Блок ]  [ Втрата ]  |  [ Фол П ]  [ Фол Т ]  [ Фол Б ]
```

**Кнопки и цвета:**

| Кнопка | Цвет | Функция |
|--------|------|---------|
| Блок | Синій #1a3560 | addBlock() |
| Втрата | Червоний #5e1515 | addTurnover() |
| Фол П | Янтарный #6b4e10 | addFoul() + счётчик фолов команды |
| Фол Т | Янтарный #6b4e10 | addFoulTechnical() |
| Фол Б | Янтарный #6b4e10 | **модальное окно** → "Скільки штрафних? [1] [2]" → addFoulShooting() + addScoreWithType(1 або 2) |

**Особенность Фол Б:**
```typescript
onClick={() => {
  setShowFoulShootingDialog(true);
  // Модальное окно:
  // "Фол при киданні - скільки штрафних?"
  // [1] [2]
  // При выборе: 
  //   - addFoulShooting(gameId, selectedTeamId, selectedPlayerId, foulCount)
  //   - Открыть окно для записи попадания/промаха
}}
```

**Дизайн:**
- Все кнопки: 36px высота, padding 6px 4px
- Разделитель: вертикальная линия 1px #1a2e40, margin 0 4px

### ЗОНА 4: Контекст броска (30px высота)

```
[ Звичайний* ]  [ 2й шанс ]  [ Швидкий ]  |  [ Фол при киданні ]
```

**Кнопки-переключатели (при клике меняют eventSubtype):**

| Кнопка | Субтип | Стиль при активности |
|--------|--------|----------------------|
| Звичайний | "normal" | Border 1px #e8a030, background светлее |
| 2й шанс | "second_chance" | Border 1px #e8a030, background светлее |
| Швидкий | "fastbreak" | Border 1px #e8a030, background светлее |
| Фол при киданні | toggle | Background янтарный #6b4e10 если активен |

**Дизайн:**
- Высота: 30px
- Padding: 6px 4px
- Активная кнопка: `border: 1px solid #e8a030` + фон светлее на 20%
- Неактивная: `border: 1px solid #1a2e40` + тёмный фон
- Разделитель перед "Фол при киданні": вертикальная 1px #1a2e40, margin 0 4px

### Что убрать
- Старую строку "ТИП КИДКА" (Звичай / Відр / 2й / Втр)
- Старую строку "ОЧКИ" (+1 / +2 / +3)
- Старые блоки "ШТРАФНІ", "СТАТИСТИКА", "ФОЛИ" как отдельные секции

### Результат
- Центральная панель полностью переструктурирована
- Все действия логически сгруппированы в 4 зоны
- Компактное размещение, всё видно без скролла

**Статус:** ⏳ К ВЫПОЛНЕНИЮ

---

## ЗАДАЧА 2: Компактный список игроков (без скролла)

### Описание
Уменьшить высоту строк в RosterPanel чтобы все 12 игроков (5 на паркеті + 7 на лавці) помещались видимыми без скролла. **Высота: 24px** (для компактности при 12 игроках).

### Файл для изменения
- `components/live-tracker/LiveScoreTracker.tsx` — функция RosterPanel (строки ~23-142)

### Требования по размерам
- **Высота каждого рядка**: 24px (strict)
- **Шрифт имени**: 11px
- **Номер**: 13px bold, min-width: 20px
- **Padding рядка**: 2px 6px
- **Margin/Gap**: 0px между рядами (без промежутков)
- **Заголовки секций**: 10px uppercase, padding 2px 4px, marginBottom 1px

### Структура RosterPanel

```typescript
// Контейнер панели
style={{
  padding: "4px 3px",
  background: isHome ? "#0d1520" : "#f3f4f6",
  fontSize: "11px",
  overflowY: "auto",  // Если что-то вылезет, скролл
  display: "flex",
  flexDirection: "column",
  minHeight: 0,
  gap: "0px"
}}

// Заголовок команды
style={{
  fontSize: "9px",
  color: isHome ? "#3a6fa5" : "#4a7fa5",
  textTransform: "uppercase",
  letterSpacing: ".4px",
  padding: "2px 4px",
  marginBottom: "1px",
  whiteSpace: "nowrap"
}}

// Секция "На паркеті"
style={{
  fontSize: "10px",
  color: isHome ? "#2ecc71" : "#059669",
  background: "transparent",
  padding: "2px 4px",
  marginBottom: "1px",
  fontWeight: "bold"
}}
// Текст: "● На паркеті ({onCourt.length})"

// Кнопка игрока
style={{
  display: "flex",
  alignItems: "center",
  gap: "4px",
  padding: "2px 6px",
  borderRadius: "2px",
  cursor: "pointer",
  background: selectedId === p.id ? (isHome ? "#163a5c" : "#fed7aa") : "transparent",
  transition: "background .1s",
  width: "100%",
  border: "none",
  fontSize: "11px",
  textAlign: "left",
  color: isHome ? (selectedId === p.id ? "#5ab3f4" : "#c8d8e8") : (selectedId === p.id ? "#92400e" : "#374151"),
  height: "24px",  // СТРОГО 24px
  lineHeight: "20px",
  margin: 0
}}

// Точка на паркеті (зелёная)
style={{
  width: "5px",
  height: "5px",
  borderRadius: "50%",
  background: isHome ? "#2ecc71" : "#059669",
  flexShrink: 0
}}

// Номер игрока
style={{
  minWidth: "20px",
  fontWeight: "bold"
}}
// Текст: "#{p.number}"

// Фамилия и инициал
style={{
  whiteSpace: "nowrap",
  overflow: "hidden",
  textOverflow: "ellipsis",
  maxWidth: "60px"
}}
// Текст: "{p.lastName} {p.firstName[0]}."
```

### Расчёт видимости (для разрешения 1366×768)
```
Верх страницы: 60px (шапка с командами)
Центральная панель: ~270px (все зоны + имя игрока)
Боковые панели игроков: calc(768 - 60px - 270px - 80px) = 358px
На паркеті (5 игроков × 24px): 120px
Лавка (7 игроков × 24px): 168px
Заголовки + разделители: ~50px
ИТОГО: ~338px — **ПОМЕЩАЕТСЯ БЕЗ СКРОЛЛА** ✅
```

### Особенность: overflowY: auto
- Если по какой-то причине игроки вылезут — будет скролл справа (не hidden!)
- Это гарантирует, что никто не будет скрыт/обрезан

### Результат
- Все 12 игроков видны без скролла
- Компактное отображение (24px × 12 = 288px)
- Touch targets достаточные для мобиля (24px высота)
- Выбранный игрок выделяется цветом #163a5c (синий фон)

**Статус:** ⏳ К ВЫПОЛНЕНИЮ

---

## ЗАДАЧА 3: PDF-протокол по стандарту FIBA

### Описание
Генерация официального FIBA протокола матча с 17 колонками, расширенной статистикой команды, тайм-аутами и точным форматированием как на скриншоте.

### Структура PDF (на основе скриншота)

#### ШАПКА
```
ЛОГОТИП | LVIV WARRIORS vs LVIV MEDICAL CENTER | ЛОГОТИП
Дата: 18.04.2026 | Место: Залік №81 | Игра №28
```

#### ТАБЛИЦА ИГРОКОВ (для каждой команды)
17 колонок:
1. № - номер
2. ГРАВЕЦЬ - имя
3. ВОЛ. - амплуа (F, G, C)
4. ХВ - минуты (формат: MM:SS)
5. ОЧК - FGM (field goals made)
6. КД - FGA (field goals attempted)
7. %КД - FG% = (FGM/FGA)*100
8. 2-ОЧК - 2PM (2-pointers made)
9. %-2ОЧК - 2P% = (2PM/2PA)*100
10. 3-ОЧК - 3PM
11. %-3ОЧК - 3P%
12. ШТ - FTM (free throws made)
13. %ШТ - FT%
14. ПД(н) - OREB (offensive rebounds)
15. ПД(з) - DREB (defensive rebounds)
16. ПД - REB (total rebounds = OREB + DREB)
17. ПР - STL (steals)
18. ВТ - TO (turnovers)
19. БЛК - BLK (blocks)
20. ФЛ - PF (personal fouls)

#### РАЗДЕЛЕНИЕ ИГРОКОВ
- **Стартовая пятёрка** — синий фон (#1a3a6b)
- **Запасные** — белый фон
- **Тренер/помічник** — отдельная строка (если добавлены)
- **Итоговая строка** — светло-серый, суммы по команде

#### ЗАГАЛЬНА СТАТИСТИКА КОМАНДИ (синий блок)
```
| Очки після втрат | Очки з трисекундної | Очки другого шансу |
| Очки у швидкому відриві | Очки поза запасом | Найбільший відрив | Найбільший ризик |
```
- Брать из `game.ptsOffTurnovers`, `game.ptsFastBreak`, `game.ptsSecondChance`, `game.biggestLead`, `game.biggestRun`

#### ТАЙМ-АУТЫ (по половинам)
```
1-я половина: ТО: 1/2 (по каждой команде отдельно)
2-я половина: ТО: 1/2
```

#### ПОДПИСИ
```
Комісар | Розшифрування
Судді (если есть поле в БД)
```

#### ЛЕГЕНДА
```
ВОЛ. = Амплуа | %КД = відсоток двохочкових | ПД(н) = підбирання у нападі
ПД(з) = підбирання у захисті | ПР = Перехоплення | БЛК = Блоки | ВТ = Втрати | ФЛ = Фолы
```

### Файлы для создания/изменения
1. **Backend**: `/app/api/games/[id]/export/fiba-pdf/route.ts` (NEW)
   - Использовать PDFKit или puppeteer
   - HTML-шаблон с данными игроков + расширенной статистики
   - A4 landscape, Arial 8-9px

2. **Frontend**: Кнопка "Завантажити FIBA Протокол" в шапке `/admin/games/[id]/page.tsx`
   - Вызывает `GET /api/games/{id}/export/fiba-pdf`
   - Скачивает PDF с именем `protokol-{homeTeam}-vs-{awayTeam}-{date}.pdf`

3. **Helper функция**: `/lib/fiba-protocol-builder.ts` (NEW)
   - `buildFibaProtocolData(game, boxScores)` → структурированные данные
   - Расчёт всех процентов
   - Агрегация по командам

### Требования к данным
- ✅ BoxScore: все колонки существуют (fg, 2p, 3p, ft, rebounds, assists, steals, blocks, fouls)
- ✅ Game: advanced stats существуют (ptsOffTurnovers, ptsFastBreak, etc.)
- ⚠️ **Нужно добавить**: Модель для тайм-аутов (пока есть счётчик в UI, но не в БД)
- ⚠️ **Нужно добавить**: Поле `isStarter` в BoxScore для выделения стартовой пятёрки

### Дизайн PDF (на основе скриншота)

#### Шрифты и размеры
- **Заголовки**: Arial 12px bold
- **Заголовки столбцов таблицы**: Arial 10px bold
- **Данные в таблице**: Arial 9px
- **Лог действий**: Arial 8px monospace
- **Подписи внизу**: Arial 8px

#### Цветовая схема таблицы
- **Стартовая пятёрка** — синий фон: #1a3a6b, белый текст
- **Запасные** — белый фон, чёрный текст
- **Тренер/помічник** — светло-серый фон, чёрный текст
- **Итоговая строка команды** — светло-серый фон (#e5e7eb), чёрный текст, **жирный шрифт**
- **Расширенная статистика команды** — синий фон #1a3a6b, белый текст

#### Структура таблицы (все 17 колонок)
| № | ГРАВЕЦЬ | ВОЛ. | ХВ | ОЧК | КД | %КД | 2-ОЧК | %-2ОЧК | 3-ОЧК | %-3ОЧК | ШТ | %ШТ | ПД(н) | ПД(з) | ПД | ПР | ВТ |

Ширина колонок (в пт):
- №: 25pt
- ГРАВЕЦЬ: 100pt (с выравниванием влево)
- ВОЛ.: 30pt
- ХВ: 35pt (формат MM:SS)
- Остальные числовые: 28pt каждая

#### Расширенная статистика команды (таблица внизу)
```
╔════════════════════════════════════════════════════════════╗
║             ЗАГАЛЬНА СТАТИСТИКА КОМАНДИ (СИНІЙ ФОН)       ║
╠════════════════════════════════════════════════════════════╣
║ Очки після втрат: X  │ Очки з трисекундної: X │ Очки другого шансу: X
║ Очки у швидкому відриві: X  │ Очки поза запасом: X  │ Найбільший відрив: X  │ Найбільший ризик: X
╚════════════════════════════════════════════════════════════╝
```

**Размеры и шрифты:**
- Заголовок: Arial 11px bold, синий фон #1a3a6b, белый текст
- Данные: Arial 9px, белый текст на синем фоне

#### Тайм-ауты (в шапке перед таблицей)
```
Тайм-ауты | 1-я половина: 2/2 | 2-я половина: 2/2
```
Для каждой команды отдельно показывается: "Використано / Доступно"

#### Подписи и легенда (внизу)
```
Комісар: ________________     Розшифрування: ________________
Судді: ___________________

ЛЕГЕНДА:
ВОЛ. = Амплуа (F/G/C) | %КД = Відсоток двохочкових
ПД(н) = Підбирання у нападі | ПД(з) = Підбирання у захисті
ПР = Перехоплення | БЛК = Блоки | ВТ = Втрати | ФЛ = Фолы
```

#### Порядок информации в PDF
1. Логотип FIBA + название соревнования
2. Дата, место, номер игры
3. **Команда 1** (названияе в шапке, финальный счёт по четвертям)
4. Таблица игроков команды 1 (стартовая пятёрка + запасные + тренер + итого)
5. Расширенная статистика команды 1 (синий блок)
6. Тайм-ауты команды 1
7. **Команда 2** (аналогично 3-6)
8. Подписи и легенда (внизу)

### Результат
- PDF протокол в официальном FIBA формате
- Все 17 колонок присутствуют и правильно отформатированы
- Расширенная статистика команды видна (синий блок)
- Тайм-ауты отображаются по половинам
- Стартовая пятёрка выделена синим фоном
- Скачивается в браузер с правильным именем файла: `protokol-{homeTeamShort}-vs-{awayTeamShort}-{date}.pdf`
- A4 landscape ориентация
- Все таблицы читаемые и профессионально оформлены

**Статус:** ⏳ К ВЫПОЛНЕНИЮ

---

## ЗАДАЧА 4: Автотесты для PDF-генерации

### Описание
Написать тесты (Jest/Vitest), проверяющие корректность PDF протокола.

### Файл для создания
- `/tests/pdf-generation.test.ts` (NEW)
- Используется Jest (в конфиге next.config.ts)

### Тест-кейсы

#### Test 1: Все колонки присутствуют
```typescript
test('FIBA PDF contains all 17 required columns', async () => {
  const { pdf } = await generateFibaPdf(mockGame);
  expect(pdf).toContain('ОЧК');
  expect(pdf).toContain('%КД');
  expect(pdf).toContain('ПД(н)');
  // ... все 17 колонок
});
```

#### Test 2: Проценты считаются правильно
```typescript
test('FG% calculated correctly (FGM/FGA * 100)', async () => {
  const { data } = await buildFibaProtocolData(mockGame);
  const player = data.players[0];
  const expectedFg = (player.fgMade / player.fgAttempted * 100).toFixed(0);
  expect(player.fgPercent).toBe(expectedFg);
});

test('2P% and 3P% calculations', () => {
  // Similar checks for 2p% and 3p%
});

test('FT% calculations', () => {
  // Free throw percentage
});
```

#### Test 3: Суммы по команде совпадают
```typescript
test('Team totals match sum of player stats', async () => {
  const { data } = await buildFibaProtocolData(mockGame);
  const teamTotal = data.teamTotals;
  const playerSum = data.players.reduce((sum, p) => sum + p.points, 0);
  expect(teamTotal.points).toBe(playerSum);
  
  // Check rebounds, assists, etc.
  expect(teamTotal.rebounds).toBe(
    data.players.reduce((sum, p) => sum + p.rebounds, 0)
  );
});
```

#### Test 4: Тайм-ауты записаны
```typescript
test('Timeouts per team per half recorded correctly', async () => {
  const { data } = await buildFibaProtocolData(mockGameWithTimeouts);
  expect(data.homeTeam.timeouts.first_half).toBe(1);
  expect(data.homeTeam.timeouts.second_half).toBe(1);
  expect(data.awayTeam.timeouts.first_half).toBe(2);
  expect(data.awayTeam.timeouts.second_half).toBe(0);
});
```

#### Test 5: Стартовая пятёрка помечена
```typescript
test('Starting five are marked correctly', async () => {
  const { data } = await buildFibaProtocolData(mockGame);
  const starters = data.players.filter(p => p.isStarter);
  expect(starters).toHaveLength(5);
  starters.forEach(player => {
    expect(player.minutes).toBeGreaterThan(0); // Starter должен был играть
  });
});
```

#### Test 6: Расширенная статистика команды
```typescript
test('Team advanced stats present and correct', async () => {
  const { data } = await buildFibaProtocolData(mockGame);
  expect(data.homeTeam.advancedStats).toHaveProperty('pointsOffTurnovers');
  expect(data.homeTeam.advancedStats).toHaveProperty('fastBreakPoints');
  expect(data.homeTeam.advancedStats).toHaveProperty('secondChancePoints');
});
```

### Результат
- ✅ Все тесты проходят
- ✅ Покрытие критических функций > 85%
- ✅ PDF генерируется без ошибок
- ✅ Данные корректны и полны

**Статус:** ⏳ К ВЫПОЛНЕНИЮ

---

## ПЛАН ВЫПОЛНЕНИЯ (по приоритету)

| # | Задача | Файлы | Время | Статус |
|---|--------|-------|-------|--------|
| 1 | Переработка центральной панели | LiveScoreTracker.tsx | 2-3 часа | ⏳ |
| 2 | Компактный список игроков | RosterPanel в LiveScoreTracker | 1 час | ⏳ |
| 3a | Добавление полей в БД (isStarter, timeouts) | schema.prisma + migration | 30 мин | ⏳ |
| 3b | FIBA PDF-генерация (backend) | /api/games/[id]/export/fiba-pdf + lib | 3-4 часа | ⏳ |
| 3c | Кнопка скачивания PDF (frontend) | /admin/games/[id]/page.tsx | 30 мин | ⏳ |
| 4 | Автотесты | /tests/pdf-generation.test.ts | 2 часа | ⏳ |

**Общее время:** ~10-12 часов разработки

**Рекомендуемый порядок:**
1. Задача 1 + 2 (UI) — параллельно
2. Задача 3 (БД + PDF) — после UI
3. Задача 4 (тесты) — в конце

---

## ЗАВИСИМОСТИ И РИСКИ

### Зависимости
- Текущие поля в BoxScore должны остаться неизменными
- Game.ptsOffTurnovers, Game.ptsFastBreak должны быть вычисляемыми из events
- Нужна поддержка isStarter в BoxScore

### Риски
- ❌ Тайм-ауты сейчас хранятся только в UI state (homeTimeouts, awayTimeouts), не в БД
  - **Решение**: Добавить GameTimeout таблицу или поле в Game
- ❌ Минуты игроков (minutes) сейчас не вычисляются из подстановок
  - **Решение**: Реализовать auto-calculation из GameSubstitution
- ❌ PDF-генерация требует headless browser (puppeteer) или хорошую HTML→PDF библиотеку
  - **Решение**: Используем PDFKit + html2canvas (уже в зависимостях)

### Точки интеграции
1. **Server Actions**: `addScore()`, `addFoul()`, etc. в `/actions/game.ts`
2. **Events API**: GET `/api/games/[id]/events` для аудита
3. **Prisma**: Обновление schema.prisma для новых полей
4. **UI**: LiveScoreTracker.tsx как основной компонент

---

## ЧЕКЛІСТ ЗАВЕРШЕНИЯ

### Задача 1: Переработка панели (дизайн + функциональность)

**Структура и кнопки:**
- [ ] ЗОНА 1 (Управление, 36px): Пауза, Період, Тайм-аут, Скасувати, Завершити
- [ ] ЗОНА 2 (Броски, 80px, 2 ряда): +2/+3 попав (зелень), ×2/×3 промах (красный), подборы, штрафные
- [ ] ЗОНА 3 (Фолы, 36px): Блок, Втрата, Фол П/Т/Б с модалью при Фол Б
- [ ] ЗОНА 4 (Контекст, 30px): Звичайний, 2й шанс, Швідкий, Фол при киданні (переключатели)
- [ ] Убрано старое структурирование (ТИП КИДКА, ОЧКИ, ШТРАФНІ, СТАТИСТИКА, ФОЛИ)

**Дизайн:**
- [ ] Все кнопки: padding 6px 4px, font-weight 600, border-radius 5
- [ ] Цвета правильные: зелень попадания, красный промахи, синий нейтраль, янтарь служебные
- [ ] Разделители между группами: вертикальные 1px #1a2e40, margin 0 4px
- [ ] Разделители между зонами: горизонтальные 1px #1a2e40, padding 4px 0
- [ ] Активные кнопки контекста: border 1px solid #e8a030, background светлее
- [ ] Всё помещается на 1366×768 без прокрутки (центр ~270px)

**Функциональность:**
- [ ] При нажатии кнопки очков — записывается в БД и лог
- [ ] При нажатии "Фол Б" — модальное окно "Скільки штрафних?"
- [ ] Тайм-аут — уменьшает счётчик команды и записывает событие
- [ ] Скасувати — вызывает undoLastEvent, удаляет последнее событие
- [ ] Контекст броска (eventSubtype) передаётся в addScore

### Задача 2: Компактный список игроков (без скролла)

**Размеры:**
- [ ] Высота рядка: 24px (строго)
- [ ] Шрифт: 11px
- [ ] Номер: 13px bold, min-width 20px
- [ ] Padding: 2px 6px
- [ ] Gap между рядами: 0px

**Видимость:**
- [ ] Все 12 игроков видны без скролла (5 паркет + 7 лавка)
- [ ] Заголовки секций: 10px uppercase, padding 2px 4px
- [ ] Выбранный игрок: фон #163a5c, цвет #5ab3f4

**Дизайн:**
- [ ] Точка паркета: зелёная (#2ecc71)
- [ ] Точка лавки: серая (#4a7fa5)
- [ ] overflowY: auto на контейнере (если вдруг вылезет)
- [ ] Никакого overflow: hidden

### Задача 3: FIBA PDF-протокол

**БД подготовка:**
- [ ] Добавлено поле `isStarter` в BoxScore
- [ ] Добавлена таблица для тайм-аутов (или расширено Game)
- [ ] Миграция Prisma запущена: `npx prisma migrate dev`

**Backend API:**
- [ ] Создан маршрут `/api/games/[id]/export/fiba-pdf`
- [ ] Функция `buildFibaProtocolData()` в `/lib/fiba-protocol-builder.ts`
- [ ] Расчёт всех процентов (FG%, 2P%, 3P%, FT%)
- [ ] Агрегация по командам (суммы, тайм-ауты)
- [ ] PDFKit генерирует PDF на основе HTML-шаблона

**Frontend:**
- [ ] Кнопка "Завантажити FIBA Протокол" в шапке `/admin/games/[id]`
- [ ] Вызывает `GET /api/games/{id}/export/fiba-pdf`
- [ ] Скачивается файл: `protokol-{homeTeam}-vs-{awayTeam}-{date}.pdf`

**Содержание PDF (проверка):**
- [ ] Логотип + название соревнования
- [ ] Дата, место, номер игры
- [ ] Две команды с таблицами игроков (17 колонок)
- [ ] Стартовая пятёрка синим фоном (#1a3a6b)
- [ ] Запасные белым фоном
- [ ] Итоговая строка команды светло-серым и жирным
- [ ] Расширенная статистика команды (синий блок) — очки, фастбрейки, подборы, рассеивание
- [ ] Тайм-ауты по половинам для каждой команды
- [ ] Подписи (комісар, судді) и легенда внизу
- [ ] A4 landscape, Arial шрифты (12pt заголовки, 9pt данные)

**Проценты и суммы:**
- [ ] FG% = (FGM/FGA) * 100, округление до целых
- [ ] 2P% = (2PM/2PA) * 100
- [ ] 3P% = (3PM/3PA) * 100
- [ ] FT% = (FTM/FTA) * 100
- [ ] REB = OREB + DREB
- [ ] Суммы по команде = сумма по всем игрокам

### Задача 4: Автотесты

**Тест-кейсы (в `/tests/pdf-generation.test.ts`):**
- [ ] Test 1: FIBA PDF содержит все 17 колонок (ОЧК, КД, %КД, 2-ОЧК, %-2ОЧК, 3-ОЧК, %-3ОЧК, ШТ, %ШТ, ПД(н), ПД(з), ПД, ПР, ВТ, ...)
- [ ] Test 2: FG% считается правильно (FGM/FGA × 100)
- [ ] Test 3: 2P%, 3P%, FT% считаются правильно
- [ ] Test 4: Суммы по команде совпадают с суммой по игрокам (очки, ребаунды, ассисты, фолы)
- [ ] Test 5: Тайм-ауты записаны по половинам для каждой команды
- [ ] Test 6: Стартовая пятёрка помечена (isStarter = true) и выделена синим в PDF

**Покрытие:**
- [ ] Все основные функции тестированы (> 85% покрытия)
- [ ] Тесты проходят локально (npm test)
- [ ] CI/CD интеграция (если настроена)

---

## ИТОГОВАЯ СВОДКА

### Что уже сделано (в предыдущих сессиях)
✅ Phase 5: Система разблокирования достижений  
✅ Phase 4: Исправлены 6 критических логических багов (рейтинг, standings, rebounds, leaders)  
✅ Phase 3: Переработана шапка дашборда с цветами и улучшен лог  
✅ Полная FIBA-совместимая БД (BoxScore со всеми метриками)  
✅ Event logging система (GameEvent таблица)  
✅ PDF-генерация (PDFKit + html2canvas)  

### Что делаем сейчас (на этой сессии)
🎯 **Задача 1**: Переработка центральной панели в 4 зоны с единым дизайном  
🎯 **Задача 2**: Компактный список игроков (24px высота, все 12 видны)  
🎯 **Задача 3**: FIBA PDF-протокол (17 колонок, расширенная статистика)  
🎯 **Задача 4**: Автотесты для проверки корректности PDF  

### Ключевые дизайн-параметры
- **Цвета**: зелень (#1e5c35) = успех, красный (#5e1515) = ошибка, синий (#1a3560) = нейтраль, янтарь (#6b4e10) = служебные
- **Размеры**: ЗОНА 1: 36px, ЗОНА 2: 80px, ЗОНА 3: 36px, ЗОНА 4: 30px
- **Разрешение**: 1366×768 (всё без прокрутки, кроме лога и списка игроков)
- **Шрифты**: 11px для кнопок, 10px для заголовков, 9px для лога

### Примерное время выполнения
| Задача | Время | Статус |
|--------|-------|--------|
| 1. Переработка панели | 2-3 часа | ⏳ |
| 2. Компактный список | 1 час | ⏳ |
| 3a. БД (isStarter, timeouts) | 30 мин | ⏳ |
| 3b. PDF backend | 3-4 часа | ⏳ |
| 3c. PDF frontend | 30 мин | ⏳ |
| 4. Тесты | 2 часа | ⏳ |
| **ИТОГО** | **~10-12 часов** | |

### Файлы для изменения (основные)
- `/components/live-tracker/LiveScoreTracker.tsx` — основной UI (центральная панель + RosterPanel)
- `/actions/game.ts` — server actions (addScore, addFoul, recordTimeoutEvent, etc.)
- `/prisma/schema.prisma` — добавить isStarter, GameTimeout модель
- `/lib/fiba-protocol-builder.ts` — NEW, помощник для генерации PDF
- `/app/api/games/[id]/export/fiba-pdf/route.ts` — NEW, PDF endpoint
- `/app/admin/games/[id]/page.tsx` — добавить кнопку скачивания
- `/tests/pdf-generation.test.ts` — NEW, автотесты

### Ready-to-go checklist
- ✅ План одобрен и детализирован
- ✅ Дизайн-требования определены и согласованы
- ✅ Скриншот FIBA протокола проанализирован
- ✅ Tech stack изучен (Next.js 14, Prisma, PDFKit, html2canvas)
- ✅ Существующая БД совместима с FIBA стандартом
- ✅ Все функции в server actions уже есть (addScore, addFoul, etc.)
- ✅ Лог событий работает корректно

### NEXT STEPS

1. **Сейчас**: ✅ План утверждён (этот документ)
2. **Далее**: Начинаем с Задачи 1 (переработка панели кнопок)
   - Парсим LiveScoreTracker.tsx
   - Реструктурируем CENTER контейнер на 4 зоны
   - Применяем дизайн-правила (padding, цвета, разделители)
3. **Параллельно**: Задача 2 (компактный список)
   - Уменьшаем высоту RosterPanel до 24px
   - Проверяем, что все 12 игроков видны
4. **После UI**: Задача 3 (FIBA PDF)
   - Обновляем БД: isStarter, timeouts
   - Создаём /api/games/[id]/export/fiba-pdf
   - Добавляем кнопку в шапку
5. **В конце**: Задача 4 (тесты)
   - Пишем 6 основных тест-кейсов
   - Проверяем покрытие > 85%

**Статус**: 🚀 ГОТОВО К ВЫПОЛНЕНИЮ
