╔═══════════════════════════════════════════════════════════════════════════╗
║              🚀 DEPLOYMENT QUICK START - basketball.lviv.ua               ║
║                        Copy & Paste Your Way to Production                ║
╚═══════════════════════════════════════════════════════════════════════════╝


STEP 1: VERIFY LOCAL BUILD (2 minutes)
═══════════════════════════════════════════════════════════════════════════

cd D:/n8n/basket-lviv
npm run build

Expected output:
  ✓ Compiled successfully
  ✓ Database has been seeded


STEP 2: TEST LOCALLY (3 minutes)
═══════════════════════════════════════════════════════════════════════════

# Start dev server in terminal 1
npm run dev:safe

# In terminal 2, test API:
curl -X POST http://localhost:3006/api/teams/add \
  -H "Content-Type: application/json" \
  -d '{"name":"Quick Start Test"}'

Expected: 201 Created


STEP 3: VERIFY SUPABASE CONNECTION (1 minute)
═══════════════════════════════════════════════════════════════════════════

node test-connection.js

Expected: CONFIGURATION SUCCESSFUL


STEP 4: GIT OPERATIONS (2 minutes)
═══════════════════════════════════════════════════════════════════════════

git status
# Should show clean or changes you want to commit

git add .

git commit -m "feat: deploy Supabase integration to production"


STEP 5: DEPLOY TO VERCEL (3-5 minutes)
═══════════════════════════════════════════════════════════════════════════

# Option A: Push to git (if CI/CD configured)
git push origin main

# Option B: Use Vercel CLI
vercel deploy --prod

# Monitor deployment
vercel logs --follow


STEP 6: VERIFY PRODUCTION (5 minutes)
═══════════════════════════════════════════════════════════════════════════

# Test production API
curl -X POST https://basketball.lviv.ua/api/teams/add \
  -H "Content-Type: application/json" \
  -d '{"name":"Production Test"}'

Expected: 201 Created

# Check Supabase Dashboard
https://app.supabase.com
→ Select project: dzsvgyetmdgykmujmxuu
→ Tables → teams
→ Verify new record appears

# Check Vercel logs for errors
vercel logs

Expected: No error messages


═══════════════════════════════════════════════════════════════════════════

✅ ALL STEPS COMPLETE = PRODUCTION LIVE!

═══════════════════════════════════════════════════════════════════════════


TOTAL TIME: ~15-20 minutes from start to production ✅


ENVIRONMENT VARIABLES STATUS
═══════════════════════════════════════════════════════════════════════════

✅ Supabase (Production):
   NEXT_PUBLIC_SUPABASE_URL = https://dzsvgyetmdgykmujmxuu.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY = sb_publishable_086iusJsMoX5QOr6FxqKFA_WBM1LMdB

✅ Auth:
   NEXTAUTH_URL = https://basketball.lviv.ua
   NEXTAUTH_SECRET = (configured)
   AUTH_SECRET = (configured)

✅ Database:
   DATABASE_URL = (Neon)
   DATABASE_URL_UNPOOLED = (Neon)
   PRISMA_DATABASE_URL = (Neon)

✅ Services:
   TELEGRAM_BOT_TOKEN = (configured)
   TELEGRAM_ADMIN_CHAT_ID = (configured)
   TELEGRAM_CHANNEL_ID = (configured)
   NEXT_PUBLIC_MONOBANK_JAR_ID = (configured)


IF SOMETHING BREAKS
═══════════════════════════════════════════════════════════════════════════

Quick Rollback (< 1 minute):
  https://vercel.com/dashboard
  → basketball.lviv.ua
  → Deployments
  → Select previous deployment
  → "..." menu
  → "Promote to Production"

Check Logs:
  vercel logs --follow

Verify Environment:
  vercel env list


IMPORTANT NOTES
═══════════════════════════════════════════════════════════════════════════

1. Keep dev server running during local tests (npm run dev:safe)

2. After git push, Vercel auto-deploys (if CI/CD configured)
   OR use: vercel deploy --prod

3. Environment variables must be in .env.local for local testing

4. For production, they're set in Vercel Dashboard:
   Settings → Environment Variables

5. NEXTAUTH_URL is automatically updated to:
   https://basketball.lviv.ua

6. Supabase credentials are public-safe (NEXT_PUBLIC_*)


WHAT WAS CONFIGURED
═══════════════════════════════════════════════════════════════════════════

✅ /api/teams/add endpoint
   - Uses production Supabase credentials
   - Targets "teams" table correctly
   - Handles POST requests → INSERT into DB

✅ AddTeam React component
   - Uses /api/teams/add endpoint
   - Client component with 'use client'
   - Form with validation

✅ Environment variables
   - All loaded from .env.local
   - Ready for Vercel production
   - NEXTAUTH_URL updated to production domain

✅ Build configuration
   - Next.js 14.2 optimized
   - Prisma migrations included
   - Ready for Vercel Functions


TESTING CHECKLIST
═══════════════════════════════════════════════════════════════════════════

Before deploying, verify:

✅ npm run build completes without errors
✅ Local API test returns 201 Created
✅ node test-connection.js shows "CONFIGURATION SUCCESSFUL"
✅ git status shows clean working tree
✅ Supabase Dashboard shows new test records
✅ No TypeScript errors
✅ No console errors when visiting https://localhost:3006


PRODUCTION VERIFICATION
═══════════════════════════════════════════════════════════════════════════

After deployment, verify:

✅ https://basketball.lviv.ua loads without error
✅ /api/teams/add responds to GET (200 OK)
✅ /api/teams/add responds to POST (201 Created)
✅ New teams appear in Supabase dashboard
✅ Vercel logs show no errors
✅ Response time < 500ms
✅ Authentication pages load correctly
✅ Database queries working


USEFUL COMMANDS
═══════════════════════════════════════════════════════════════════════════

Build:
  npm run build

Test locally:
  npm run dev:safe

Test API:
  curl -X POST http://localhost:3006/api/teams/add \
    -H "Content-Type: application/json" \
    -d '{"name":"Test"}'

Git:
  git status
  git add .
  git commit -m "message"
  git push origin main

Vercel:
  vercel status
  vercel logs --follow
  vercel deployments list
  vercel open


═══════════════════════════════════════════════════════════════════════════

Ready to deploy? Start with STEP 1 above!

For detailed info, see: PRODUCTION_DEPLOYMENT_GUIDE.md

═══════════════════════════════════════════════════════════════════════════
