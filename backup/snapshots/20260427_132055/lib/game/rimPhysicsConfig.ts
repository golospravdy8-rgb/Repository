// Конфиг физики кольца — масштабируется автоматически под размер канвы
// rimRadius в пикселях = 0.23 * (canvasWidth / 6.75)

export const RIM_PHYSICS_CONFIG = {
  // Геометрия кольца (в пикселях — будет обновлено при инициализации)
  rimRadius: 27,              // ≈ 0.23м баскетбольного кольца
  rimWidth: 7,                // толщина дужки

  // Физика отскока (взято из LukeJW0/Pygame + NBA реализм)
  rimRestitution: 0.25,       // упругость при отскоке (0.0 = no bounce, 1.0 = perfect)
  rimFriction: 0.82,          // трение по дужке (замедляет тангенциальное движение)

  // Демпирование скорости
  linearDamping: 0.08,        // линейное затухание после каждого обновления
  angularDamping: 0.18,       // угловое затухание (вращение)

  // Вратарские зоны (в пикселях от центра кольца)
  // Срабатывают когда мяч проходит через них
  topGateOffset: -14,         // верхняя зона (мяч входит в кольцо сверху)
  bottomGateOffset: 26,       // нижняя зона (мяч выходит через сетку → ГОЛ!)
  gateWidth: 40,              // ширина проёма для входа/выхода

  // Пороги скорости для различных типов ударов
  minVelocityTopGate: 0.2,    // мяч должен лететь вниз минимум на эту скорость
  minVelocityBottomGate: 0.4, // при выходе снизу — более строгий порог
} as const;
