# 🔴 ЗВІТ АУДИТУ ФУНКЦІОНАЛУ basketball.lviv.ua

**Дата:** 2026-04-18  
**Статус:** ❌ КРИТИЧНІ БАГИ ВИЯВЛЕНІ

---

## ✅ ПРАЦЮЄ ПРАВИЛЬНО

- ✅ **Standings сортування:** Правильна логіка (wins DESC, pointsFor DESC)
- ✅ **Гри та матчі:** Логіка завантаження та статусу LIVE/FINAL функціонує
- ✅ **Побудова команд:** Команди завантажуються коректно по сезонам
- ✅ **Таблиця дифференціалу:** +/- розраховується як (pointsFor - pointsAgainst)

---

## 🐛 КРИТИЧНІ БАГИ

### БАГ 1: ДВІСЛЯВ ФОРМУЛИ РЕЙТИНГУ — КОНФЛІКТ ФУНКЦІЙ

**Де:** 
- `lib/achievements.ts` рядок 17-26 (функція `calculateRating`)
- `lib/stats-calculator.ts` рядок 85 (функція `calculateLeaderStats`)

**Проблема:**
Існує **ДВОРІВНЕВА система розрахунку рейтингу** з РІЗНИМИ формулами:

#### Формула 1: `calculateRating()` з achievements.ts (НЕВІРНА)
```javascript
rating = (points×1 + rebounds×1.2 + assists×1.5 + steals×2 + blocks×2) / games
```
- Коефіцієнти НЕВІДПОВІДНІ задекларованій формулі на сайті
- **Немає базового +50**
- ОЧ коефіцієнт: 1.0 замість 1.8
- БЛК коефіцієнт: 2.0 замість 1.8

#### Формула 2: `calculateLeaderStats()` з stats-calculator.ts (ПРАВИЛЬНА)
```javascript
rating = Math.min(99, Math.round(50 + (points/g)×1.8 + (rebounds/g)×1.2 + (assists/g)×1.5 + (steals/g)×2.0 + (blocks/g)×1.8))
```
- Має базове +50 ✅
- Коефіцієнти відповідають сайту ✅
- Обмежена максимум 99 ✅

**Де використовується:**
- Сторінка гравців (`players/page.tsx` рядок 51): `calculateRating()` ← НЕВІРНА
- Сторінка гравця (ID) (`players/[id]/page.tsx` рядок 40): `calculateRating()` ← НЕВІРНА
- Сторінка лідерів (`leaders/page.tsx` рядок 27): `calculateLeaderStats()` ← ПРАВИЛЬНА

**Контрольні розрахунки:**

| Гравець | ОЧ | РЕБ | ПЕР | ПМ | БЛК | Формула 1 (achievements) | Формула 2 (stats-calculator) | На сайті | Статус |
|---------|----|----|-----|----|----|-------------------------|------------------------------|---------|--------|
| Г.Мальцев | 20 | 0 | 11 | 4 | 4 | (20×1+0+11×1.5+4×2+4×2)/n = зависит от n | 50+36+0+16.5+8+7.2 = 117.7 | 53 | ❌ |
| К.Осмуха | 24 | 0 | 7 | 3 | 2 | зависит от n | 50+43.2+0+10.5+6+3.6 = 113.3 | Лідери: 99, Гравці: 45 | ❌ |

**Очікується:** Обидві функції мають використовувати ОДНУ правильну формулу з stats-calculator.ts

---

### БАГ 2: РОЗБІЖНІСТЬ РЕЙТИНГУ КИРИЛА ОСМУХИ

**Де:** 
- Сторінка лідерів (`/leaders`): показує рейтинг **99**
- Сторінка гравця (`/players/[id]`): показує рейтинг **45**
- Топ-5 гравців (`/players`): показує рейтинг **45**

**Проблема:**
Один гравець має **ДВА РІЗНІ РЕЙТИНГИ** залежно від сторінки:
- Використовується `calculateLeaderStats()` → 99 (правильна)
- Використовується `calculateRating()` → 45 (неправильна)

**Причина:** Різні функції розрахунку в одному додатку

**Очікується:** Всюди один рейтинг = **99** (за правильною формулою)

---

### БАГ 3: ДОСЯГНЕННЯ (ACHIEVEMENTS) НЕ СИНХРОНІЗУЮТЬСЯ

**Де:** `actions/game.ts` рядки 64-65, 179-180, 272-296

**Проблема:**
Функція `syncAchievements()` **закоментована** (рядок 272-296):
```typescript
// TODO: playerAchievement model not in schema yet
// async function syncAchievements(playerId: number): Promise<string[]> {
//   ...
// }
```

Всі функції що додають события (addScore, addAssist, addRebound...) мають:
```typescript
// TODO: playerAchievement model not in schema yet
// const newAchievements = await syncAchievements(playerId);
...
return { newAchievements: [] };
```

**Наслідок:**
- Досягнення **НІКОЛИ** не розблоковуються автоматично
- Функція `checkNewAchievements()` існує в `achievements.ts` але **НІКОЛИ** не викликається
- Гравці можуть не мати досягнень навіть якщо виконали умови

**Очікується:**
1. Розкоментувати `syncAchievements()` у game.ts
2. Викликати її після кожного event'у що змінює box score
3. Автоматично створювати PlayerAchievement записи при розблокуванні

---

### БАГ 4: НЕВІДПОВІДНІСТЬ УМОВ ДОСЯГНЕНЬ (achievements.ts)

**Де:** `lib/achievements.ts` рядок 9-15

**Проблема:**
На сайті на сторінці гравців заявлено **7 досягнень**:
```
🏹 Снайпер: 20+ очок в одній грі
⭐ Трипл-дабл: 10+ очок, підбирань І передач в одній грі
🛡️ Залізний захист: 5+ блок-шотів в одній грі
🎯 Розігруючий: 7+ передач в одній грі
💪 Король підбирань: 10+ підбирань в одній грі
📈 Стабільний: 10+ очок у 3 іграх поспіль
🤝 Командний гравець: середнє 5+ передач за весь сезон
```

Але в коді ЛИШЕ **5 досягнень**:
```typescript
{ id: "top-scorer",      name: "Снайпер",           ... }
{ id: "team-player",     name: "Командний гравець",  ... description: "5+ асистів в одній грі" }
{ id: "defender",        name: "Захисник",           ... description: "3+ перехоплень в одній грі" }
{ id: "rebounder",       name: "Підбирач",           ... description: "10+ підборів в одній грі" }
{ id: "iron-man",        name: "Залізна людина",     ... description: "Зіграв 5+ ігор" }
```

**Розбіжності:**
1. ❌ Немає **Трипл-дабл** (повинен бути: 10+ очок, 10+ підбирань, 10+ передач в одній грі)
2. ❌ Немає **Залізного захисту** по блокам (замість цього — 3+ перехоплень)
3. ❌ Немає **Розігруючого** (7+ передач в одній грі)
4. ❌ Немає **Стабільного** (10+ очок у 3 іграх поспіль)
5. ✅ **Снайпер** є (20+ очок)
6. ⚠️ **Командний гравець** має умову "5+ асистів в одній грі", а не "середнє 5+ за весь сезон"

---

### БАГ 5: НЕПОВНА РЕАЛІЗАЦІЯ ЛОГІКИ ПІДБИРАННЯ

**Де:** Prisma schema (рядок 155-156) та LeadersSection

**Проблема:**
- Schema має **ЛИШ ОДНЕ** поле: `rebounds: Int`
- Але логіка пробує розрізнювати:
  - `reboundsOff` (offensive rebounds) — рядок 155
  - `reboundsDef` (defensive rebounds) — рядок 156
- Функція `game.ts` використовує обидва поля (рядки 211, 216)
- Але контролер на сторінці лідерів показує ТІЛЬКИ `rpg` як загальні підбирання

**Результат:**
Гавців з 0 підборами мають 0 RPG, але тим не менше можуть бути в топ-5 або навіть лідерами залежно від других статистик. МАКСИМ МАНЗЯК зі 0 підборів є лідером підборів — це **АНОМАЛІЯ**.

---

### БАГ 6: СИСТЕМА HP ЗАДЕКЛАРОВАНА АЛЕ НЕ РЕАЛІЗОВАНА

**Де:** 
- `players/page.tsx` рядки 217-244 — показує таблицю HP системи
- Schema: `GuestContact` має поле `hp` (рядок 228)
- Але **ніде не видно логіки нарахування HP**

**Задекларована логіка:**
```
Реєстрація в чат: +25 HP
Щоденне відвідування: +15 HP
Реферал: +50 HP
Амбасадор: 100 HP
Рекрутер: 300 HP
Легенда ліги: 1000 HP
```

**Проблема:**
- Нема функцій що нараховують HP при реєстрації
- Нема логіки для щоденного відвідування (+15 HP один раз на 24 год)
- Нема реферальної системи
- HP у профілю гавців показується як СТАТИЧНА лічба, чи вона динамічна?

---

### БАГ 7: НЕВІДПОВІДНІСТЬ +/- В BOX SCORE

**Де:** 
- `actions/game.ts` рядки 493-518 (функція `addScoreWithType`)
- Комбініровуються ВСІ гавці які були на паркеті під час гола

**Проблема:**
- +/- розраховується за всіма на-паркеті гавцями
- **НО** логіка передбачає, що спочатку гавець зривається (+points), потім ВСІ на-паркеті оновлюються
- Це означає **той ж гавець що забив** отримує **ПОДВІЙНИЙ пририст** +/-:
  1. Як гавець що забив: +points
  2. Як на-паркеті гавець: +points ще раз

**Очікується:** Гавець що забив не повинен входити у "updateOnCourtOps"

---

## ⚠️ ПІДОЗРІЛІ МІСЦЯ

1. **Коефіцієнти БЛК:** У achievements.ts БЛК має 2.0, а має бути 1.8
2. **Максимум рейтингу 99:** У stats-calculator обмежено до 99, але як статистика покаже більше?
3. **Не дистабільовується:** Чи хто розпочинає синхронізацію dosahizhen при редагуванні гри?
4. **Команди з однаковим W-L:** Якщо дві команди мають однакові wins і pointsFor, в якому порядку вони?
5. **MVP визначення:** На сторінці гравців MVP = гравець з найвищим рейтингом, але рейтинги невірні
6. **Поле "tier":** LeaderStats має поле `tier`, але воно залежить від `getRatingTier()` яка змінилася

---

## 📊 ПЕРЕВІРКА КОНКРЕТНИХ ДАНИХ

### Рейтинг — відповідність формулі

| Гравець | Форм. на сайті | Коефіцієнти (сайт) | Коефіцієнти (код) | Приклад розр. | Очікується | На сайті (Лідери) | На сайті (Гравці) | Статус |
|---------|---|---|---|---|---|---|---|---|
| Г.Мальцев | 50+ОЧ×1.8+РЕБ×1.2+ПЕР×1.5+ПМ×2.0+БЛК×1.8 | ОЧ:1.8, РЕБ:1.2, ПЕР:1.5, ПМ:2.0, БЛК:1.8 | ОЧ:1.0, РЕБ:1.2, ПЕР:1.5, ПМ:2.0, БЛК:2.0 | 20,0,11,4,4 | 50+36+0+16.5+8+7.2=117.7 | ~53 | 53 | ❌ |
| К.Осмуха | 50+ОЧ×1.8+РЕБ×1.2+ПЕР×1.5+ПМ×2.0+БЛК×1.8 | ОЧ:1.8, РЕБ:1.2, ПЕР:1.5, ПМ:2.0, БЛК:1.8 | ОЧ:1.0, РЕБ:1.2, ПЕР:1.5, ПМ:2.0, БЛК:2.0 | 24,0,7,3,2 | 50+43.2+0+10.5+6+3.6=113.3 | 99 | 45 | ❌ |

### Лідери підборів — аномалія

| Гравець | Підбирання | На РПГ | На сайті Лідер | Статус |
|---------|---|---|---|---|
| Максим Манзяк | 0 | 0 | **ДА (Лідер підборів)** | ❌ НЕВІРНО |

---

## 🔧 ПРІОРИТЕТНІ ВИПРАВЛЕННЯ

### 1. 🔴 КРИТИЧНО: Об'єднати функції рейтингу
- **Проблема:** `calculateRating()` vs `calculateLeaderStats()` — дві різні формули
- **Вирішення:** Замінити ВЕЗДЕ `calculateRating()` на правильну логіку з `calculateLeaderStats()`
- **Файли:**
  - Видалити `calculateRating()` з `lib/achievements.ts`
  - Замінити у `app/(public)/players/page.tsx` рядок 51
  - Замінити у `app/(public)/players/[id]/page.tsx` рядок 40
- **Код:**
  ```typescript
  // Замість calculateRating(p.boxScores)
  // Використовувати правильну формулу:
  const avgPts = p.boxScores.length ? (p.boxScores.reduce((s, g) => s + g.points, 0) / p.boxScores.length) : 0;
  const avgReb = p.boxScores.length ? (p.boxScores.reduce((s, g) => s + g.rebounds, 0) / p.boxScores.length) : 0;
  // ... та далі як у stats-calculator.ts рядок 85
  ```

### 2. 🔴 КРИТИЧНО: Включити синхронізацію досягнень
- **Проблема:** `syncAchievements()` закоментована, досягнення ніколи не розблоковуються
- **Вирішення:**
  1. Розкоментувати функцію в `actions/game.ts` рядки 272-296
  2. Викликати після кожного stat event'у
  3. Замінити всі `return { newAchievements: [] }` на `return { newAchievements }`
- **Файли:** `actions/game.ts`

### 3. 🔴 КРИТИЧНО: Додати відсутні досягнення
- **Проблема:** 7 досягнень на сайті, але 5 в коді
- **Вирішення:** Додати до BADGES:
  ```typescript
  { id: "triple-double", name: "Трипл-дабл", icon: "⭐", description: "10+ очок, підбирань і передач в одній грі", color: "#6366f1" },
  { id: "shot-blocker", name: "Залізний захист", icon: "🛡️", description: "5+ блок-шотів в одній грі", color: "#22c55e" },
  { id: "playmaker", name: "Розігруючий", icon: "🎯", description: "7+ передач в одній грі", color: "#f59e0b" },
  { id: "consistent", name: "Стабільний", icon: "📈", description: "10+ очок у 3 іграх поспіль", color: "#8b5cf6" },
  ```
- Файли: `lib/achievements.ts`

### 4. 🟡 ВАЖЛИВО: Виправити коефіцієнти в achievements.ts
- **Проблема:** `calculateRating()` має неправильні коефіцієнти
- **Вирішення:** 
  ```typescript
  // Було (НЕВІРНО):
  points * 1 + rebounds * 1.2 + assists * 1.5 + steals * 2 + blocks * 2
  // Має бути:
  50 + points * 1.8 + rebounds * 1.2 + assists * 1.5 + steals * 2.0 + blocks * 1.8
  ```
- **Файли:** `lib/achievements.ts` рядок 22

### 5. 🟡 ВАЖЛИВО: Виправити +/- подвійне рахування
- **Проблема:** Гавець що забив отримує +/- дважды
- **Вирішення:** Виключити гавця що забив з `updateOnCourtOps`
- **Файли:** `actions/game.ts` рядки 498-504

### 6. 🟡 ВАЖЛИВО: Реалізувати логіку HP нарахування
- **Проблема:** HP задекларована але не реалізована
- **Вирішення:** Додати функції для нарахування HP при реєстрації, денному відвідуванні, рефералах
- **Файли:** Нові функції в `actions/` або API route

### 7. 🟢 БАЖАНО: Виправити фільтрацію лідерів підборів
- **Проблема:** Гавці з 0 підборів можуть бути лідерами
- **Вирішення:** Перевірити логіку в LeadersSection.tsx сортування
- **Файли:** `components/public/LeadersSection.tsx` рядок 20

---

## 📋 СПИСОК ФАЙЛІВ ДЛЯ ПЕРЕВІРКИ

- ✅ `lib/achievements.ts` — ПРОЧИТАНИЙ, знайдено 2 баги
- ✅ `lib/stats-calculator.ts` — ПРОЧИТАНИЙ, формула правильна
- ✅ `actions/game.ts` — ПРОЧИТАНИЙ, синхронізація закоментована
- ✅ `app/(public)/players/page.tsx` — ПРОЧИТАНИЙ, використовує неправильну формулу
- ✅ `app/(public)/players/[id]/page.tsx` — ПРОЧИТАНИЙ, використовує неправильну формулу
- ✅ `app/(public)/leaders/page.tsx` — ПРОЧИТАНИЙ, використовує правильну формулу
- ✅ `components/public/LeadersSection.tsx` — ПРОЧИТАНИЙ, сортування корректне
- ✅ `components/public/StandingsTable.tsx` — ПРОЧИТАНИЙ, сортування корректне
- ✅ `prisma/schema.prisma` — ПРОЧИТАНИЙ, структура правильна

---

## 🎯 ВИСНОВОК

**Стан системи:** 🔴 **НЕБЕЗПЕЧНО ДЛЯ PRODUCTION**

Виявлено **7 основних багів**, з них:
- **3 критичних** (рейтинги невірні, досягнення не працюють, формули конфліктують)
- **2 важливих** (коефіцієнти невірні, +/- рахується неправильно)
- **2 бажаних** (HP не реалізована, лідери підборів фільтруються неправильно)

**Ефект на користувачів:**
1. MVP визначається за невірним рейтингом
2. Гавці не отримують досягнення хоча виконали умови
3. Рейтинги різні на різних сторінках
4. HP система задекларована але не працює

**Рекомендація:** Негайно виправити критичні баги перед наступною live грою.

